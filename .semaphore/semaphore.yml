version: v1.0
name: Test MyApp
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Build
    task:
      jobs:
        - name: 'Eslint, install, ...'
          commands:
            - cache clear
            - checkout
            - sem-version node 12
            - cache restore
            - npm install
            - cache store
            - 'curl https://install.meteor.com/ | sh'
            - cache delete meteor-gemx
            - cache store meteor-gemx /usr/local/bin/
            - ls
    dependencies: []
  - name: Test
    dependencies:
      - Build
    task:
      jobs:
        - name: Run unit test
          commands:
            - 'curl https://install.meteor.com/ | sh'
            - npm run test
      prologue:
        commands:
          - checkout
          - cache restore
  - name: Deploy
    dependencies:
      - Test
    task:
      jobs:
        - name: Using mup to deploy
          commands:
            - checkout
            - cache restore
            - 'curl https://install.meteor.com/ | sh'
            - npm install --global mup
            - ls -a
            - cd .deploy
            - mup setup
            - mup deploy
